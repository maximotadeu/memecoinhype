services:
  - type: web
    name: telegram-token-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py  # ou app.py dependendo do seu arquivo
    envVars:
      - key: TELEGRAM_TOKEN
        fromSecret: true
      - key: CHAT_ID
        fromSecret: true
