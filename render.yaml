services:
  - type: web
    name: telegram-token-monitor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        fromSecret: true
      - key: CHAT_ID
        fromSecret: true
